import React from 'react';
import './shapes.css'

const Circle = (props) => {
    return (
        <div>
            <div className='circle'>{props.shee}</div>
        </div>
    );
}

export default Circle;
